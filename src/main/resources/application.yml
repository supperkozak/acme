logging:
 level:
   ROOT: INFO
server:
 port: 8080
 context-path: /

spring:
  jpa:
    generate-ddl: false
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
  datasource:
    platform: mysql
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/acme
    password: root
    username: root